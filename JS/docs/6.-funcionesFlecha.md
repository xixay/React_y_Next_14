- [1. funciones](#1-funciones)
  - [1.1. funcion normal](#11-funcion-normal)
  - [1.2. funcion flecha](#12-funcion-flecha)
    - [1.2.1. Un solo parametro](#121-un-solo-parametro)
    - [1.2.2. Sin parametros](#122-sin-parametros)
    - [1.2.3. Multiples parametros](#123-multiples-parametros)
    - [1.2.4. Un solo return](#124-un-solo-return)
    - [1.2.5. Retornar un string](#125-retornar-un-string)
      - [1.2.5.1. Un solo parametro](#1251-un-solo-parametro)
      - [1.2.5.2. Un solo return](#1252-un-solo-return)
    - [1.2.6. otros ejemplos](#126-otros-ejemplos)
      - [1.2.6.1. Sin paréntesis (simplificada)](#1261-sin-paréntesis-simplificada)
      - [1.2.6.2. Con paréntesis (opcional para mejorar legibilidad o manejar estructuras complejas):](#1262-con-paréntesis-opcional-para-mejorar-legibilidad-o-manejar-estructuras-complejas)
      - [1.2.6.3. Uso obligatorio de parentesis](#1263-uso-obligatorio-de-parentesis)
    - [1.2.7. Valores por defecto](#127-valores-por-defecto)
# 1. funciones
## 1.1. funcion normal
```js
function sumar(num) {
  console.log(10);
}
sumar(10); //10
```
## 1.2. funcion flecha
```js
const sumarDos = (num1, num2) => {
  console.log(num1 + num2);
};
sumarDos(20, 30); //50
```
### 1.2.1. Un solo parametro
- con solo un parametro se puede omitir parentesis
```js
const sumarTres = (num) => {
  console.log(num);
};
sumarTres(20); //20
```
### 1.2.2. Sin parametros
- Los parentesis son obligatorios
```js
const saludar = () => console.log("Hola");
saludar(); //'Hola'
```
### 1.2.3. Multiples parametros
- si se tiene return esta devolviendo un valor
```js
const sumarCuatro = (num1, num2) => {
  return num1 + num2;
};
const resultado = sumarCuatro(20, 30);
console.log(resultado); //50
```
### 1.2.4. Un solo return
- si solo tiene un return se puede omitir las llaves
```js
const sumarCinco = (num1, num2) => num1 + num2;
const resultadoDos = sumarCinco(20, 30);
console.log(resultadoDos); //50
```
### 1.2.5. Retornar un string
- retornar string
```js
const mensaje = () => {
  return 'hola soy xixay';
};
const resultadoTres = mensaje();
console.log(resultadoTres); //'hola soy xixay'
```
#### 1.2.5.1. Un solo parametro
- si se tiene solo un parametro se puede omitir los parentesis
```js
const mensajeDos = nombre => {
  return 'hola soy ' + nombre;
};
const resultadoCuatro = mensajeDos('Yumiko');
console.log(resultadoCuatro); //'hola soy Yumiko'
```
#### 1.2.5.2. Un solo return
- si se tiene un solo return se puede omitir los parentesis y el return
```js
const mensajeTres = nombre => 'hola soy ' + nombre;
const resultadoCinco = mensajeTres('sunset');
console.log(resultadoCinco); //'hola soy sunset'
```
### 1.2.6. otros ejemplos
#### 1.2.6.1. Sin paréntesis (simplificada)
- Esta es la forma más concisa y común cuando la función tiene un solo retorno y no necesita estructuras complejas:
```js
const mensaje = (nombre) => 'Hola soy ' + nombre;

const resultado = mensaje('Shimmer');
console.log(resultado); // 'Hola soy Shimmer'
```
#### 1.2.6.2. Con paréntesis (opcional para mejorar legibilidad o manejar estructuras complejas):
- Los paréntesis son útiles para claridad visual, especialmente cuando se trabaja con múltiples líneas o estructuras complejas como objetos o JSX:
```js
const mensaje = (nombre) => (
  'Hola soy ' + nombre
);

const resultado = mensaje('Shimmer');
console.log(resultado); // 'Hola soy Shimmer'
```
#### 1.2.6.3. Uso obligatorio de parentesis
- Cuando retornas un objeto literal:
```js
const crearObjeto = () => ({
  clave: 'valor',
  numero: 42,
});
console.log(crearObjeto()); // { clave: 'valor', numero: 42 }
```
- Cuando retornas JSX en React:
```js
const MiComponente = () => (
  <div>
    <h1>Hola, soy un componente</h1>
  </div>
);
```
### 1.2.7. Valores por defecto
- valores por defecto para que no este undefined
```js
//por defecto num sera uno
const sumaSeis = (num = 1) => {
  console.log(num + 3); //4
};
sumaSeis();
```
