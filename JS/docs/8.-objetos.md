- [1. objetos](#1-objetos)
  - [1.1. Accediendo a las propiedades](#11-accediendo-a-las-propiedades)
  - [1.2. Agregando propiedades dinámicamente](#12-agregando-propiedades-dinámicamente)
# 1. objetos
- Un objeto es una colección de propiedades y valores. Las propiedades pueden ser datos primitivos, arrays, funciones u otros objetos.
```js
const mascota = {
  nombre: 'Tom',
  edad: 10,
  vivo: true,
  razas: ['peludo', 'negro'],
};
console.log(mascota); //{ nombre: 'Tom', edad: 10, vivo: true, razas: [ 'peludo', 'negro' ] }
```
## 1.1. Accediendo a las propiedades
```js
console.log(mascota.nombre); //Tom
console.log(mascota.edad); //10
console.log(mascota.vivo); //true
```
## 1.2. Agregando propiedades dinámicamente
- Puedes añadir nuevas propiedades a un objeto existente de forma dinámica:
```js
mascota.id = 1;
console.log(mascota); //{ nombre: 'Tom', edad: 10, vivo: true, razas: [ 'peludo', 'negro' ], id: 1 }
```
