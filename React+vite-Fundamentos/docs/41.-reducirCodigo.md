- [1. Reducir Codigo](#1-reducir-codigo)

## 1. Reducir Codigo
- Uso de desestructuración para simplificar componentes.
- Simplificación de componentes mediante el retorno implícito en funciones de una sola línea.
- Mejora de la legibilidad y reducción de código redundante.
```jsx
import imgUno from './assets/images/img-1.jpg'

const MyButton = ({ text }) => {
  // console.log(props)
  return (
    <button>{text}</button>
  )
 }
// Desestructurando y devolviendo JSX en una sola línea
const WelcomeText = ({user}) => user ? <h3>Online</h3>: <h3>Offline</h3>

// Componente para renderizar elementos de una lista
const ItemFrut = ({index, frut}) => {
  return (
    <li key={index}>{frut} - {index}</li>
  )
}
const App = () => {

  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  // const pathImg = 'src/assets/images/img-1.jpg';
  const user = true;

  const fruts =['manzana', 'platano', 'pera'];

  return (
    <>
      <h1 className={classTitle}>{title}</h1>
      <img src={imgUno} alt={`imagen-${title}`} />

      <MyButton text = 'botón 1'/>
      <MyButton text = 'botón 2'/>
      <MyButton text = 'botón 3'/>
      <WelcomeText user = {user}/>
      <ul>
        {
          fruts.map((frut, index) => (
            // <li key={index}>{frut} - {index}</li>//Sera remplazado
            <ItemFrut key = {index} frut = {frut}/>
          ))
        }
      </ul>
    </>
  )
};

export default App;
```
