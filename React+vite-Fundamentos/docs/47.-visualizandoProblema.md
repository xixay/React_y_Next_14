- [1. Visualizando el Problema](#1-visualizando-el-problema)
  - [1.1. Contexto](#11-contexto)
  - [1.2. Diagnóstico](#12-diagnóstico)
  - [1.3. Solución: Usar useState](#13-solución-usar-usestate)
  - [1.4. Código Corregido](#14-código-corregido)

# 1. Visualizando el Problema
## 1.1. Contexto
- En el componente ButtonState, el contador (count) no se actualiza visualmente en el botón cuando se hace clic. Aunque el valor del contador se incrementa y se registra en la consola, React no re-renderiza el componente porque no detecta cambios en el estado.
## 1.2. Diagnóstico
- El problema ocurre porque:
  - count es una variable local y no forma parte del estado del componente.
  - React re-renderiza el componente solo cuando se actualiza su estado o sus props.
- En este caso, actualizar count directamente con count = count + 1 no informa a React que debe re-renderizar el componente.
## 1.3. Solución: Usar useState
- Para resolver el problema, debemos utilizar el hook useState para manejar el estado del contador.
## 1.4. Código Corregido
- ButtonState.jsx
```jsx
const ButtonState = () => {
  //hacer un contador
  let count = 0;
  const handleClick = () => {
    count = count + 1;
    console.log('handleClick', count);
  };
  return <button onClick={handleClick}>buttonState: {count}</button>;
};
export default ButtonState;
```
- Importar el nuevo boton
```jsx
import imgUno from './assets/images/img-1.jpg';

import MyButton from './components/MyButton';
import WelcomeText from './components/WelcomeText';
import ListFruts from './components/fruts/ListFruts';
import ButtonState from './components/ButtonState';

const App = () => {
  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  // const pathImg = 'src/assets/images/img-1.jpg';
  const user = false;

  const fruts = ['manzana', 'platano', 'pera'];
  const frutsTwo = ['naranja', 'melon', 'sandia'];

  return (
    <>
    <ButtonState/>
      <h1 className={classTitle}>{title}</h1>
      <img src={imgUno} alt={`imagen-${title}`} />

      <MyButton text="botòn 1" />
      <MyButton text="botòn 2" />
      <MyButton text="botòn 3" />
      <WelcomeText user={user} />
      <ListFruts fruts={fruts} />
      <ListFruts fruts={frutsTwo} />
    </>
  );
};

export default App;
```
- El problema resulta que al querer mostralo en el boton el count que se realizo un cambio, en react ya se renderizo una vez, cuando el contador estaba recien en cero
**![Normal](/React+vite-Fundamentos/imagenes/47.-elProblema.png)**
