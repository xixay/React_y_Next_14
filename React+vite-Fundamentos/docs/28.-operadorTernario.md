# Operador ternario
## Renderizado condicional
- Es un if y un else, que se usa en js
- como la cte user es true, mostrara el valor Online
```jsx
const MyButton = () => {
  return (
    <button>Soy un boton</button>
  )
 }

const App = () => {

  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  const pathImg = 'https://picsum.photos/200/300';
  const user = true;

  return (
    <>
      <h1 className={classTitle}>{title}</h1>
      <img src={pathImg} alt={`imagen-${title}`} />
      <MyButton></MyButton>
      {/* pregunta si usuario es verdadero */}
      {
        user ? 'Online' : 'Offline'
      }
    </>
  )
};

export default App;
```
**![Normal](/React+vite-Fundamentos/imagenes/28.-ternario.png)**