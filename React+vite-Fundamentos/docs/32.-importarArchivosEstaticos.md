- [1. Importar Archivos Estáticos en React con Vite](#1-importar-archivos-estáticos-en-react-con-vite)
  - [1.1. Guardar la Imagen en el Proyecto](#11-guardar-la-imagen-en-el-proyecto)
  - [1.2. Intento con Rutas Relativas](#12-intento-con-rutas-relativas)
  - [1.3. Uso de Rutas Absolutas Relativas al Proyecto](#13-uso-de-rutas-absolutas-relativas-al-proyecto)
  - [1.4. Importación Directa de la Imagen (Recomendada)](#14-importación-directa-de-la-imagen-recomendada)

# 1. Importar Archivos Estáticos en React con Vite
## 1.1. Guardar la Imagen en el Proyecto
- Crea una carpeta llamada images dentro de src/assets y guarda la imagen como img-1.jpg.
## 1.2. Intento con Rutas Relativas
- Se intentó usar una ruta relativa para referenciar la imagen, pero esto no funcionó correctamente en Vite.
```jsx
const pathImg = './assets/images/img-1.jpg'; // No se encuentra.
```
```jsx
const MyButton = () => {
  return (
    <button>Soy un boton</button>
  )
 }

const OnlineText = () =>{
  return (
    <div>
      <h3>Online</h3>
    </div>
  )
}
const OfflineText = () =>{
  return (
    <div>
      <h3>Offline</h3>
    </div>
  )
}
const App = () => {

  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  const pathImg = './assets/images/img-1.jpg';
  const user = false;

  return (
    <>
      <h1 className={classTitle}>{title}</h1>
      <img src={pathImg} alt={`imagen-${title}`} />
      <MyButton></MyButton>
      {user && <OnlineText/>}
    </>
  )
};

export default App;
```
**![Normal](/React+vite-Fundamentos/imagenes/32.-archivosEstaticos.png)**

## 1.3. Uso de Rutas Absolutas Relativas al Proyecto
- Una solución funcional es usar la ruta relativa desde src.
```jsx
const pathImg = 'src/assets/images/img-1.jpg'; // Funciona correctamente.
```
```jsx
const MyButton = () => {
  return (
    <button>Soy un boton</button>
  )
 }

const OnlineText = () =>{
  return (
    <div>
      <h3>Online</h3>
    </div>
  )
}
const OfflineText = () =>{
  return (
    <div>
      <h3>Offline</h3>
    </div>
  )
}
const App = () => {

  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  const pathImg = 'src/assets/images/img-1.jpg';
  const user = false;

  return (
    <>
      <h1 className={classTitle}>{title}</h1>
      <img src={pathImg} alt={`imagen-${title}`} />
      <MyButton></MyButton>
      {user && <OnlineText/>}
    </>
  )
};

export default App;
```
**![Normal](/React+vite-Fundamentos/imagenes/32.-archivosEstaticosImagen.png)**

## 1.4. Importación Directa de la Imagen (Recomendada)
- Vite permite importar imágenes como módulos en tu archivo JavaScript. Esto es más limpio y confiable.
```jsx
import imgUno from './assets/images/img-1.jpg';
```
```jsx
import imgUno from './assets/images/img-1.jpg'

const MyButton = () => {
  return (
    <button>Soy un boton</button>
  )
 }

const OnlineText = () =>{
  return (
    <div>
      <h3>Online</h3>
    </div>
  )
}
const OfflineText = () =>{
  return (
    <div>
      <h3>Offline</h3>
    </div>
  )
}
const App = () => {

  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  // const pathImg = 'src/assets/images/img-1.jpg';
  const user = false;

  return (
    <>
      <h1 className={classTitle}>{title}</h1>
      <img src={imgUno} alt={`imagen-${title}`} />
      <MyButton></MyButton>
      {user && <OnlineText/>}
    </>
  )
};

export default App;
```
