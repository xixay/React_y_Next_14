- [1. Reutilizando el Componente ListFruts](#1-reutilizando-el-componente-listfruts)
  - [1.1. Objetivo](#11-objetivo)
  - [1.2. Paso 1: Crear un Segundo Array de Frutas](#12-paso-1-crear-un-segundo-array-de-frutas)
  - [1.3. Paso 2: Reutilizar el Componente ListFruts](#13-paso-2-reutilizar-el-componente-listfruts)
    - [1.3.1. Contenido actualizado de App.jsx:](#131-contenido-actualizado-de-appjsx)

# 1. Reutilizando el Componente ListFruts
## 1.1. Objetivo
- Reutilizar el componente ListFruts con otro conjunto de datos (frutsTwo) para mostrar una segunda lista de frutas.
## 1.2. Paso 1: Crear un Segundo Array de Frutas
- En el archivo App.jsx, define un nuevo array frutsTwo con una lista adicional de frutas.
```jsx
 const frutsTwo = ['naranja', 'melon', 'sandia'];
```
## 1.3. Paso 2: Reutilizar el Componente ListFruts
- Llama al componente ListFruts nuevamente en el JSX, pasándole como prop el nuevo array frutsTwo.
### 1.3.1. Contenido actualizado de App.jsx:
```jsx
import imgUno from './assets/images/img-1.jpg';

import MyButton from './components/MyButton';
import WelcomeText from './components/WelcomeText';
import ListFruts from './components/fruts/ListFruts';

const App = () => {
  const title = 'Mi titulo desde una constante';
  const classTitle = 'text-center';
  // const pathImg = 'src/assets/images/img-1.jpg';
  const user = false;

  const fruts = ['manzana', 'platano', 'pera'];
  const frutsTwo = ['naranja', 'melon', 'sandia'];

  return (
    <>
      <h1 className={classTitle}>{title}</h1>
      <img src={imgUno} alt={`imagen-${title}`} />

      <MyButton text="botòn 1" />
      <MyButton text="botòn 2" />
      <MyButton text="botòn 3" />
      <WelcomeText user={user} />
      <ListFruts fruts={fruts} />
      <ListFruts fruts={frutsTwo} />
    </>
  );
};

export default App;
```
**![Normal](/React+vite-Fundamentos/imagenes/46.-tareaDos.png)**